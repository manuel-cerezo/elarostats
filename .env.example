# ──────────────────────────────────────────────
#  elarostats — environment variables template
#  Copy this file to .env and fill in the values.
# ──────────────────────────────────────────────

# Supabase project URL (visible in Project Settings → API)
PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxxxxxxxxxx.supabase.co

# Supabase anon / public key — safe to expose in the browser
# (Row Level Security on player_stats restricts writes)
PUBLIC_SUPABASE_ANON=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Supabase service-role key — NEVER commit or expose publicly.
# Only needed by the sync script (scripts/sync-players.ts).
SUPABASE_SERVICE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
